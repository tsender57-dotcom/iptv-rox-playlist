name: Scheduled M3U8 Scraper (Hybrid)

on:
  schedule:
    # Eksekusi otomatis setiap 10 menit
    - cron: '*/10 * * * *'
  workflow_dispatch: 

jobs:
  scrape_and_update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Memastikan history git terbaca penuh untuk rebase

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Playwright & Dependencies
        run: |
          npm install playwright
          npx playwright install chromium --with-deps

      - name: Run Scraper Engine
        run: node scraper.js

      - name: Commit and Push M3U Playlist
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          
          if [ -f playlist.m3u ]; then
            git add playlist.m3u
            git commit -m "Auto-Update: Camelscore M3U8 [$(date)]" || echo "No changes to commit"
            
            # LOGIKA PRO: Tarik data terbaru dari server dan gabungkan secara paksa (rebase)
            # Ini mencegah error "fetch first / rejected" jika ada perubahan manual saat script berjalan
            git pull --rebase origin main
            
            git push
          fi
          
