name: Auto Update Playlists

on:
  schedule:
    - cron: '*/30 * * * *' # Script akan berjalan otomatis setiap 30 menit
  workflow_dispatch: # Mengizinkan Anda menekan tombol "Run" manual

jobs:
  update-playlists:
    runs-on: ubuntu-latest

    # Memberikan izin kepada bot untuk menulis file ke repository
    permissions:
      contents: write 

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests bs4 playwright pytz
          playwright install --with-deps
          playwright install chromium
        
      - name: Run Roxiestreams Script
        run: python update_rox.py
        continue-on-error: true # Jika Roxie error, bot tetap akan lanjut ke Shark

      - name: Run Sharkstreams Script
        run: python update_shark.py
        continue-on-error: true # Jika Shark error, bot tetap akan lanjut ke Watchfooty

      - name: Run Watchfooty Script
        run: python update_watchfooty.py
        continue-on-error: true # Jika Watchfooty error, bot tetap akan lanjut ke Commit

      - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # JURUS SAPU BERSIH: Tambahkan semua file .m3u8 yang berhasil dibuat
          git add *.m3u8
          
          # Cek apakah ada perubahan file
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          
          git commit -m "Auto-update Roxie, Shark, & WFTY playlists"
          
          # Push dengan aman (Safe Push)
          if ! git push origin main; then
            echo "Push rejected — rebasing safely..."
            git stash
            git pull --rebase origin main
            git stash pop
            git add *.m3u8
            git commit -m "Auto-update Roxie, Shark, & WFTY playlists (rebased)" || true
            git push origin main
          fi
